@using SharedClasses.Models;

<h3>@(Badge?.Title ?? "NoTitle")</h3>
<p> <img src="UploadedImages/@(Badge?.FileName??"Placeholder_view_vector.svg")" height="100" /></p>
<h2>Description</h2>
@* <p>@((MarkupString)Markdig.Markdown.ToHtml(Badge?.Description ?? ""))</p>
<h2>Turn in instructions</h2>
<p>@((MarkupString)Markdig.Markdown.ToHtml(Badge?.TurnInInstructions ?? ""))</p> *@

@code {
    //TODO: Replace Markdig with Blazor Bootstrap
    [Parameter]
    public HSBadge? Badge { get; set; }
}
